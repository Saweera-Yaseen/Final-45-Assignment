//storing a famous quote and its author

let quote:string="Nothing is impossible because impossible itself I M possible";
let author:string="Saweera";
//printing the message 
console.log(`"${quote}" - ${author}`);